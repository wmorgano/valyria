A small Python-ish Interpreter, for educational purposes.

Supports simple self-evaluating and binary expressions with Integer, Float, and String operands.

Includes a basic object system with the ability to create a variable as an identifer bound to an Object instance for any of the supported literal types, and stores the variables in an environment for lookup and evaluation for later expressions.
